import { Injectable } from "@angular/core";

@Injectable({
	providedIn: "root",
})
export class AuthService {
	constructor() {}

	authService(user: any) {
		//check if there is user (storeData = true)
	}
}
